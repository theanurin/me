{
  "name": "@theanurin/me",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -r ./dist",
    "dev": "eleventy --serve --pathprefix=/dev",
    "build": "eleventy",
    "preview": "eleventy --serve --port=8080  --pathprefix=/dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "@iarna/toml": "^2.2.5",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "js-yaml": "^4.1.1",
    "sass": "^1.94.0"
  }
}
