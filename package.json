{
  "name": "@theanurin/me",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -r ./dist",
    "build": "BUILD_CONFIGURATION=snapshot eleventy --pathprefix=/dev",
    "dev": "  BUILD_CONFIGURATION=snapshot eleventy --pathprefix=/dev --serve --port=8080"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "@iarna/toml": "^2.2.5",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "js-yaml": "^4.1.1",
    "sass": "^1.99.0"
  }
}
